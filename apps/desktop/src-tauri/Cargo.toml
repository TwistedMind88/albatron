[package]
name = "albatron-desktop"
version = "0.1.0"
description = "Albatron poslovni sistem - desktop klijent"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-updater = "2"
tauri-plugin-process = "2"
tauri-plugin-dialog = "2"
tauri-plugin-fs = "2"
serde_json = "1"

[profile.release]
strip = true
lto = true
